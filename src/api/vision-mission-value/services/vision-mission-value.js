'use strict';

/**
 * vision-mission-value service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::vision-mission-value.vision-mission-value');
